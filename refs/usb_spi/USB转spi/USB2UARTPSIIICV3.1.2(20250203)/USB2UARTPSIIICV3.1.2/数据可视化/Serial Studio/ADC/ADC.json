{
    "actions": [
    ],
    "decoder": 1,
    "frameDetection": 2,
    "frameEnd": "\\n",
    "frameParser": "/**\n * Splits a data frame into an array of elements using a comma separator.\n *\n * Use this function to break a string (like \"value1,value2,value3\") into\n * individual pieces, which can then be displayed or processed in your project.\n *\n * @param[in]  frame   A string containing the data frame.\n *                     Example: \"value1,value2,value3\"\n *\n * @return     An array of strings with the split elements.\n *             Example: [\"value1\", \"value2\", \"value3\"]\n *\n * @note You can declare global variables outside this function if needed\n *       for storing settings or keeping state between calls.\n */\nfunction parse(frame) {\n    let dataArray = [];\n    for (let i = 0; i < frame.length; i += 4) {\n        let hexByte = frame.substring(i, i + 4);\n        let decimalValue = parseInt(hexByte, 16);\n        let decimalValue1 = decimalValue + 800;\n        dataArray.push(decimalValue);\n        dataArray.push(decimalValue1);\n    }\n\n    return dataArray;\n}\n",
    "frameStart": "$",
    "groups": [
        {
            "datasets": [
                {
                    "alarm": 0,
                    "fft": true,
                    "fftSamples": 1024,
                    "fftSamplingRate": 200,
                    "graph": true,
                    "index": 1,
                    "led": false,
                    "ledHigh": 1,
                    "log": false,
                    "max": 4096,
                    "min": 0,
                    "title": "ADC0",
                    "units": "",
                    "value": "--.--",
                    "widget": "gauge"
                },
                {
                    "alarm": 0,
                    "fft": false,
                    "fftSamples": 256,
                    "fftSamplingRate": 100,
                    "graph": true,
                    "index": 2,
                    "led": false,
                    "ledHigh": 1,
                    "log": false,
                    "max": 4096,
                    "min": 0,
                    "title": "ADC1",
                    "units": "",
                    "value": "--.--",
                    "widget": "bar"
                }
            ],
            "title": "多个绘图",
            "widget": "multiplot"
        }
    ],
    "mapTilerApiKey": "",
    "thunderforestApiKey": "",
    "title": "未命名项目"
}
